function slide(chosen) { $('.slide-box').each(function(index) { if ($(this).attr("id") == chosen) { $(this).slideToggle(200); } else { $(this).slideUp(600); } }); }	 function toggle(chosen) { $('#'+chosen).slideToggle(200); } $(window).scroll(function(){ $('#calendar-scroller').css({ 'top': $(this).scrollTop() }); }); function setCookie(c_name,value,exdays) { var exdate=new Date(); exdate.setDate(exdate.getDate() + exdays); var c_value=escape(value) + ((exdays==null) ? "" : "; expires="+exdate.toUTCString()); document.cookie=c_name + "=" + c_value; }function repost (id, text, username) { document.getElementById(id).style.display = 'none'; document.getElementById('new-post-text').value = text + ' via @' + username; } function updateUC (title, id) { document.getElementById('custom-calendar').selected = true; document.getElementById('custom-calendar').setAttribute('value', id); document.getElementById('custom-calendar').innerHTML = title; } $(document).ready(function(){ $('.dotdotdot-description').dotdotdot({ ellipsis: '... ', wrap: 'word', after: null, watch: true, height: 60 }); }); $(document).ready(function(){ $('.top-heading').html(function(i, html){ return html.replace(/([^\s]+)/, '<span class="orange">$1</span>') }) });
